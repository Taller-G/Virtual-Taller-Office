# URL pública del servidor Colyseus (ws:// en local, wss:// en producción).
# El cliente estático la lee en tiempo de build: cambiarla implica rebuild.
VITE_SERVER_URL=ws://localhost:2567

# Puerto del servidor de desarrollo de Vite (opcional)
VITE_PORT=5173

# Los mapas salen del registro de mundos (packages/shared/src/worlds.ts): cada
# mundo nombra su archivo dentro de public/assets/map/. Las imágenes de los
# tilesets se resuelven relativas a ese archivo, como en Tiled.
